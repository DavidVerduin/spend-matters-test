/** The component displayed at the top */
export const HeaderComponent = () => {
  return (
    <div style={{display: "flex", alignItems: "center", justifyContent: "center"}}>
      <span style={{fontSize: "large", fontWeight: "bold"}}>Dring Finder</span>
    </div>
  )
}