import "./header.css";

/** The component displayed at the top */
export const HeaderComponent = () => {
  return (
    <div className="header">
      <span className="header-title">Drink Finder</span>
    </div>
  )
}