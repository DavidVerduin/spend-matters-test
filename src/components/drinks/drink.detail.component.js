/**This component will appear in a modal when the element is selected */
export const DrinkDetailComponent = () => {
  // Get props
  // Get image url and display url
  // Display ingredients table (maybe component?)
  // Display instructions (maybe component?)
  return (
    <div>
      DrinkDetailComponent works!
    </div>
  )
}